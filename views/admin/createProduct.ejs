<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link href="/styles.css" rel="stylesheet" />
    <title>Create Product</title>
</head>
<body>
    <%- include('../partials/headerAdmin') %> <%- include('../partials/menuAdmin') %>
    <main class="grid grid-cols-10 mt-14">
        <section class="col-span-2"></section>
        <section class="col-span-8">
            <h1 class="text-3xl font-semibold">Add new product</h1>
            <% if (errors.length > 0) { %>
                <div class="alert alert-danger">
                    <ul>
                        <% errors.forEach(function(error) { %>
                            <li><%= error %></li>
                        <% }) %>
                    </ul>
                </div>
            <% } %>
            <form action="/admin/save" method="POST" enctype="multipart/form-data" class="mt-3 max-w-[800px] ">
                <div class="mb-3">
                    <label for="" class="form-label text-xl font-semibold">Title</label>
                    <input type="text" name="title" id="" class="form-control">
                </div>
                <div class="mb-3">
                    <label for="" class="form-label text-xl font-semibold">Price</label>
                    <input type="number" name="price" id="" class="form-control">
                </div>
                <div class="mb-3" >
                    <label for="" class="form-label text-xl font-semibold">Description</label>
                    <input type="text" name="description" id="" class="form-control">
                </div>
                <div class="mb-3">
                    <label for="" class="form-label text-xl font-semibold">Thumbnail</label>
                    <input type="file" name="thumbnail" id="" class="form-control">
                </div>
                <div class="mb-3 w-full">
                    <button type="submit" class="btn btn-primary w-full">Add product</button>
                </div>  
            </form>
        </section>
    </main>
    
</body>
</html>